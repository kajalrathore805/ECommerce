 
<div class="container d-flex justify-content-center mt-5">
  <div class="p-4 border rounded shadow bg-white" style="min-width: 400px;">
    <h2 class="text-dark mb-4 text-center">Login form</h2>

			<%= form_with model: @user, url: login_path , method: :post  do |form|%>


				<div class="mb-3 col-sm-4">
					<%= form.label :email%>
					<%= form.text_field :email,class: "form-control"%>
				</div>

				<div class="mb-3 col-sm-4">
					<%= form.label :password_digest%>
					<%= form.text_field :password_digest,class: "form-control"%>
				</div>

			  <div class="mb-3 col-sm-4">
					<%= form.submit "Submit",class: "btn btn-success" %>
				</div>
			<% end %>
	</div>
</div>
